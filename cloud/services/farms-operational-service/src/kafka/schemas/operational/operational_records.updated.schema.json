{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OperationalRecordsUpdate",
  "description": "Schema สำหรับ validate payload การอัพเดต operational_records",
  "type": "object",
  "properties": {
    "id": { "type": "integer", "description": "รหัสเรคอร์ดที่ต้องการอัพเดต" },
    "record_type": { "type": ["string", "null"], "description": "ประเภทข้อมูล (optional)" },
    "record_data": { "type": ["object", "null"], "description": "ข้อมูลรายละเอียด (optional)" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่บันทึก (optional)" },
    "effective_start": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่เริ่มผล (optional)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": [ "id" ],
  "additionalProperties": false
}