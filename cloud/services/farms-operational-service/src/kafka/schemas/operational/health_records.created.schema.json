{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HealthRecordsCreate",
  "description": "Schema สำหรับ validate payload การสร้าง health_records ใหม่",
  "type": "object",
  "properties": {
    "customer_id": { "type": "integer", "description": "รหัสลูกค้า" },
    "farm_id": { "type": "integer", "description": "รหัสฟาร์ม" },
    "animal_id": { "type": ["integer", "null"], "description": "รหัสสัตว์ (optional)" },
    "health_status": { "type": "string", "description": "สถานะสุขภาพ" },
    "note": { "type": ["string", "null"], "description": "หมายเหตุเพิ่มเติม (optional)" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่บันทึกสุขภาพ (optional)" },
    "effective_start": { "type": "string", "format": "date-time", "description": "วัน-เวลาที่เริ่มมีผล (timestamptz)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": [ "customer_id", "farm_id", "health_status", "effective_start" ],
  "additionalProperties": false
}