{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EnvironmentalFactorUpdate",
  "description": "Schema สำหรับ validate payload การอัพเดต environmental_factors",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "รหัสเรคอร์ดที่ต้องการอัพเดต"
    },
    "ventilation_rate": {
      "type": ["number", "null"],
      "description": "อัตราการระบายอากาศ (optional)"
    },
    "note": {
      "type": ["string", "null"],
      "description": "หมายเหตุเพิ่มเติม (optional)"
    },
    "measurement_date": {
      "type": ["string", "null"],
      "format": "date",
      "description": "วันที่วัด (optional)"
    },
    "effective_start": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "วัน-เวลาที่เริ่มมีผล (optional, timestamptz)"
    },
    "effective_end": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "วัน-เวลาที่สิ้นสุดผล (optional, timestamptz)"
    }
  },
  "required": ["id"],
  "additionalProperties": false
}