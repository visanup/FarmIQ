{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HousingConditionsUpdate",
  "description": "Schema สำหรับ validate payload การอัพเดต housing_conditions",
  "type": "object",
  "properties": {
    "id": { "type": "integer", "description": "รหัสเรคอร์ดที่ต้องการอัพเดต" },
    "condition_type": { "type": ["string", "null"], "description": "ประเภทสภาพ (optional)" },
    "condition_value": { "type": ["number", "string", "null"], "description": "ค่าของสภาพ (optional)" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่วัด (optional)" },
    "effective_start": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่เริ่มผล (optional)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": [ "id" ],
  "additionalProperties": false
}