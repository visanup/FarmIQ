{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HealthRecordsUpdate",
  "description": "Schema สำหรับ validate payload การอัพเดต health_records",
  "type": "object",
  "properties": {
    "id": { "type": "integer", "description": "รหัสเรคอร์ดที่ต้องการอัพเดต" },
    "health_status": { "type": ["string", "null"], "description": "สถานะสุขภาพ (optional)" },
    "note": { "type": ["string", "null"], "description": "หมายเหตุเพิ่มเติม (optional)" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่บันทึกสุขภาพ (optional)" },
    "effective_start": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่เริ่มมีผล (optional)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": [ "id" ],
  "additionalProperties": false
}