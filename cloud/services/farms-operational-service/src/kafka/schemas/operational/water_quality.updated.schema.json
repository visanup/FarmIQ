{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WaterQualityUpdate",
  "description": "Schema สำหรับ validate payload การอัพเดต water_quality",
  "type": "object",
  "properties": {
    "id": { "type": "integer", "description": "รหัสเรคอร์ดที่ต้องการอัพเดต" },
    "water_source": { "type": ["string", "null"], "description": "แหล่งน้ำ (optional)" },
    "ph": { "type": ["number", "null"], "description": "ค่า pH (optional)" },
    "turbidity": { "type": ["number", "null"], "description": "ค่าความขุ่น (optional)" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่วัด (optional)" },
    "effective_start": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่เริ่มมีผล (optional)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": ["id"],
  "additionalProperties": false
}