{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FeedIntakeUpdate",
  "description": "Schema สำหรับ validate payload การอัพเดต feed_intake",
  "type": "object",
  "properties": {
    "id": { "type": "integer", "description": "รหัสเรคอร์ดที่ต้องการอัพเดต" },
    "customer_id": { "type": ["integer", "null"], "description": "รหัสลูกค้า (optional)" },
    "farm_id": { "type": ["integer", "null"], "description": "รหัสฟาร์ม (optional)" },
    "animal_id": { "type": ["integer", "null"], "description": "รหัสสัตว์ (optional)" },
    "batch_id": { "type": ["string", "null"], "description": "รหัสชุดข้อมูล (optional)" },
    "feed_quantity": { "type": ["number", "null"], "description": "ปริมาณอาหาร (optional)" },
    "updated_at": { "type": ["string", "null"], "format": "date-time", "description": "เวลาอัพเดต (optional)" }
  },
  "required": ["id"],
  "additionalProperties": false
}