{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HousingConditionsCreate",
  "description": "Schema สำหรับ validate payload การสร้าง housing_conditions ใหม่",
  "type": "object",
  "properties": {
    "customer_id": { "type": "integer", "description": "รหัสลูกค้า" },
    "farm_id": { "type": "integer", "description": "รหัสฟาร์ม" },
    "house_id": { "type": ["integer", "null"], "description": "รหัสบ้าน (optional)" },
    "condition_type": { "type": "string", "description": "ประเภทสภาพ (เช่น ventilation, humidity)" },
    "condition_value": { "type": ["number", "string"], "description": "ค่าของสภาพ" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่วัด (optional)" },
    "effective_start": { "type": "string", "format": "date-time", "description": "วัน-เวลาที่เริ่มมีผล (timestamptz)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": [ "customer_id", "farm_id", "condition_type", "condition_value", "effective_start" ],
  "additionalProperties": false
}