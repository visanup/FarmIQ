{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OperationalRecordsCreate",
  "description": "Schema สำหรับ validate payload การสร้าง operational_records ใหม่",
  "type": "object",
  "properties": {
    "customer_id": { "type": "integer", "description": "รหัสลูกค้า" },
    "farm_id": { "type": "integer", "description": "รหัสฟาร์ม" },
    "record_type": { "type": "string", "description": "ประเภทข้อมูล (e.g., operational, maintenance)" },
    "record_data": { "type": "object", "description": "ข้อมูลรายละเอียด (dynamic JSON)" },
    "measurement_date": { "type": ["string", "null"], "format": "date", "description": "วันที่บันทึก (optional)" },
    "effective_start": { "type": "string", "format": "date-time", "description": "วัน-เวลาที่เริ่มมีผล (timestamptz)" },
    "effective_end": { "type": ["string", "null"], "format": "date-time", "description": "วัน-เวลาที่สิ้นสุดผล (optional)" }
  },
  "required": [ "customer_id", "farm_id", "record_type", "record_data", "effective_start" ],
  "additionalProperties": false
}