version: "3.8"
services:
  mock-sensor:
    build: .
    container_name: farm-mock-sensor
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - farm_edge

networks:
  farm_edge:
    external: true
    name: farmiq-edge_farm_edge

